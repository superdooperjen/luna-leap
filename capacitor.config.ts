import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.superdooperjen.lunaleap",
  appName: "Luna Leap",
  webDir: "dist",
  android: {
    backgroundColor: "#0b0b2a",
  },
};

export default config;
